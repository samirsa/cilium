apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdp
spec:
  selector:
    matchLabels:
      app: pdp
  replicas: 1
  template:
    metadata:
      labels:
        app: pdp
    spec:
      containers:
      - name: pdp
        image: docker.io/samircilium/pdp:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 10000
        - containerPort: 20000
---
apiVersion: v1
kind: Service
metadata:
  name: pdp-svc
  labels:
    app: pdp
spec:
  ports:
  - port: 10000
    targetPort: 10000
    protocol: TCP
    name: pdp-service
  - port: 20000
    targetPort: 20000
    protocol: TCP
    name: pdp-author-api
  selector:
    app: pdp
